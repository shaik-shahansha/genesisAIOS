services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  daemon:
    ports:
      - "${GENESIS_PORT:-3000}:3000"
